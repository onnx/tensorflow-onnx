# Unit test

jobs:
- template: 'templates/job_generator.yml'
  parameters:
    tf_versions: ['1.12']
    onnx_opsets: ['8', '7']
    onnx_backends:
      onnxruntime: ['0.2.1']
    job:
      steps:
      - template: 'unit_test.yml'
